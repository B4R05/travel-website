import { EXPERIENCE_DATA } from '../App/types';

export type EXPERIENCE_PROPS = {
  data: EXPERIENCE_DATA;
  loadExpData: Function;
  startBooking: Function;
};

export type ImageProps = {
  height: number;
};
